# opencode 対応

```
ai-adapter alias opencode
#=> ln -s .github(絶対パス) .opencode(絶対パス) 

ai-adapter install opencode
#=> export opencode.json at current directory

ai-adapter uninstall opencode
#=> delete opencode.json at current directory

```

